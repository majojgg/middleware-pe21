openapi: "3.1.0"

info:
  title: "UIDE Inscripciones API"
  version: "2.0.0"
  description: "API para gestión de inscripciones académicas"
  license:
    name: "MIT"
    identifier: "MIT"

servers:
  - url: http://localhost:3000
    description: "Servidor local"

paths:
  /health:
    get:
      operationId: healthCheck
      summary: "Verificar estado de la API"

      security: []

      responses:
        "200":
          description: "API funcionando correctamente"
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: "ok"
                  ts:
                    type: string
                    format: date-time
                    example: "2026-06-23T12:00:00.000Z"

        "500":
          description: "Error interno del servidor"

  /v1/inscripciones:
    post:
      operationId: crearInscripcionV1
      summary: "Registrar una inscripción académica (v1)"

      security:
        - ApiKeyAuth: []

      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InscripcionV1Request"
            example:
              estudianteID: "123"
              materias:
                - "Middleware"
              periodoID: "2026-A"

      responses:
        "201":
          description: "Inscripción creada correctamente"

        "400":
          description: "Campos requeridos: estudianteID, materias, periodoID"

        "401":
          description: "API key inválida o ausente"

        "500":
          description: "Error interno del servidor"

  /v2/inscripciones:
    post:
      operationId: crearInscripcionV2
      summary: "Registrar una inscripción académica (v2)"

      security:
        - ApiKeyAuth: []

      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InscripcionV2Request"
            example:
              estudianteID: "123"
              materias:
                - "Middleware"
              periodoID: "2026-A"
              metodo_pago: "Transferencia"

      responses:
        "201":
          description: "Inscripción creada correctamente"

        "400":
          description: "Datos inválidos o método de pago incorrecto"

        "401":
          description: "API key inválida o ausente"

        "500":
          description: "Error interno del servidor"

components:
  securitySchemes:
    ApiKeyAuth:
      type: apiKey
      in: header
      name: x-api-key

  schemas:
    InscripcionV1Request:
      type: object

      required:
        - estudianteID
        - materias
        - periodoID

      properties:
        estudianteID:
          type: string
          example: "123"

        materias:
          type: array
          minItems: 1
          items:
            type: string
          example:
            - "Middleware"

        periodoID:
          type: string
          example: "2026-A"

    InscripcionV2Request:
      type: object

      required:
        - estudianteID
        - materias
        - periodoID
        - metodo_pago

      properties:
        estudianteID:
          type: string
          example: "123"

        materias:
          type: array
          minItems: 1
          items:
            type: string
          example:
            - "Middleware"

        periodoID:
          type: string
          example: "2026-A"

        metodo_pago:
          type: string
          enum:
            - Efectivo
            - Transferencia
            - Débito
            - Crédito
          example: "Transferencia"